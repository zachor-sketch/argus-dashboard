import {sha256,timestamp,source,prediction} from './validation-lab-model.js';
export function sealPrediction(packet){const frozen=prediction(packet);return Object.freeze({packet:frozen,hash:sha256(frozen)})}
export function revealOutcome(outcome,sealed){if(!sealed?.hash||sha256(sealed.packet)!==sealed.hash)throw Error('LAB_PREDICTION_MUTATED');if(outcome.predictionHash!==sealed.hash||timestamp(outcome.revealTimestamp)<=timestamp(sealed.packet.timestamp))throw Error('LAB_FREEZE_BEFORE_REVEAL');if(timestamp(outcome.outcomeCutoff)<=timestamp(sealed.packet.evidenceCutoff))throw Error('LAB_OUTCOME_WINDOW');(outcome.sources||[]).forEach(s=>source(s,outcome.outcomeCutoff,{historical:true}));return Object.freeze(structuredClone(outcome))}
export function horizonState(due,asOf){return timestamp(asOf)<timestamp(due)?'UNRESOLVED':'DUE'}
